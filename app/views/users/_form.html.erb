<%= form_for @user, html: { multipart: true } do |f| %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image, id:"upload-here"%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
