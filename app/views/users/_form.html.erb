<%= form_for @user, html: { multipart: true } do |f| %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image, id:"upload-field", name:"uploader-field" %>
  </div>
  <div class="actions">
    <%= f.submit 'Update User', :id => 'uploader-button'%>
  </div>
<% end %>
